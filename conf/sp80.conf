# mydock configuration file
# Generated by wizard on 2025-10-10 13:26:24

# Container image
REPOSITORY=registry.lab.tail-f.com:5000/jenkins-doc
TAG=11-1804

# Container details
NAME=sp80
HOSTNAME=sp80

# Directory mapping
SHARED_DIR=/home/tobbe/work/git

# Port mappings
PORTS=9080:8080
EXTRA_RUN_FLAGS="-p 9008:8008"

# ========================================
# User Mirroring Configuration
# ========================================
# Enable automatic user mirroring
MIRROR_USER=yes

# User settings (auto-detected if not specified)
MIRROR_USER_NAME=tobbe
MIRROR_USER_UID=1000
MIRROR_USER_GID=1000

# Directories to mount with matching paths
MIRROR_DIRS=(
  "/home/tobbe/.gitconfig:ro"
  "/home/tobbe/.ssh:ro"
  "/home/tobbe/.bashrc:ro"
)

# ========================================
# Additional Notes
# ========================================
# To use this configuration:
#   ./mydock run --config <this-file>
#
# To preview the docker command:
#   ./mydock run --config <this-file> --dry-run
#
# To enter the container:
#   ./mydock shell --name <container-name>
#   ./mydock user --name <container-name> --user <username>
